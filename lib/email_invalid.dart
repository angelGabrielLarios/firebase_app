import 'package:flutter/material.dart';

class EmailInvalid extends StatelessWidget {
  const EmailInvalid({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Email invalid'),
      ),
      body: Container(),
    );
  }
}
